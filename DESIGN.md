# Design System

## Brand

**mobile-store** — ร้านขายสินค้าไอทีและแกดเจ็ต (โน้ตบุ๊ก มือถือ หูฟัง อุปกรณ์เสริม) ขายออนไลน์และมีบัญชีลูกค้ากับตัวแทนจำหน่ายที่ถือยอดคงเหลือไว้ล่วงหน้า คอนโซลนี้คือหลังบ้านการเงินของร้าน สโลแกน "TECH FOR A BETTER YOU"

โลโก้เป็นตราวงกลม อักษร M ทองไล่เฉดบนพื้นเอสเปรสโซ ใช้เฉดเดียวกับ `--primary` และ `--sidebar` วาดเป็น inline SVG ที่ `app/components/brand-mark.tsx` แทน `next/image` จึงคมทุก DPI และไม่ต้องยิงรีเควสต์รูปเพิ่ม (id ของ gradient กันชนด้วย `useId` เพราะตราเรนเดอร์สองที่)

favicon ใช้ `app/icon.svg` ซึ่งเป็นภาพเดียวกัน — file convention ของ Next รองรับ `.svg` และเสิร์ฟไฟล์ตามจริงโดยไม่ optimize ไฟล์จึงต้องเล็กอยู่แล้ว (711B)

## Direction

ภาพของโต๊ะปฏิบัติการการเงินร้านไอทีช่วงสายภายใต้แสงสำนักงานที่สว่าง: ข้อมูลนิ่ง ชัด แม่นยำ และสงบ สีทองอมบรอนซ์เป็นลายเซ็นเดียวของระบบ ให้ความอบอุ่นและน่าเชื่อถือโดยไม่ล้าตาเมื่อมองนาน

## Color Strategy

Minimal restrained product palette พื้นสว่างเจือ hue ทอง 85 ทองใช้กับ primary action, current selection และ focus ring เท่านั้น สีเขียวและแดงสงวนให้ความหมายฝาก–ถอน แถบนำทางเป็น neutral ชั้นที่สองแบบเข้ม โทนเอสเปรสโซเจือทอง เพื่อรองรับตราวงกลมสีสว่าง

ทองคุม chroma ไว้ต่ำ (~0.115) เพื่อไม่ให้แสบตา และแยกตามบทบาท ไม่ใช่ตามความสว่าง:

- `--primary` ทองสว่าง ใช้กับเส้นขอบ hover, sparkline และ accent ที่ไม่แบกความหมาย
- `--primary-hover` ทองเข้ม ใช้กับเส้นขอบและจุดที่สื่อความหมาย เพราะทองสว่างบนพื้นขาวได้แค่ 2.49:1 ไม่ถึงเกณฑ์ 3:1 ของ non-text
- `--primary-strong` ทองบรอนซ์เข้ม ใช้กับตัวอักษรและปุ่มทึบที่วางตัวอักษรสีขาว

โทน warning/pending อยู่ที่ hue ~58 (ส้มอำพัน) แยกจากทองแบรนด์ hue ~85 เพื่อไม่ให้ status กลืนกับ accent

ทุกคู่สีข้อความหลักผ่าน WCAG AA (ต่ำสุด 6.28:1) และ focus ring ได้ 4.52:1

```css
--bg: oklch(0.984 0.005 85);
--surface: oklch(0.966 0.009 85);
--ink: oklch(0.215 0.018 85);
--muted: oklch(0.48 0.015 85);
--primary: oklch(0.72 0.115 88);
--primary-hover: oklch(0.64 0.105 86);
--primary-strong: oklch(0.44 0.075 82);
--sidebar: oklch(0.245 0.022 80);
--success: oklch(0.455 0.105 158);
--danger: oklch(0.455 0.145 25);
```

## Typography

ใช้ Noto Sans Thai Variable ทั้งระบบ ตัวเลขใช้ tabular numerals เพื่อให้ตารางและยอดเงินเทียบกันได้ง่าย ขนาดหัวเรื่องคงที่ตามรูปแบบ product UI และไม่มี display typography ที่รบกวนงาน

## Layout

Desktop ใช้ sidebar 232px และพื้นที่ทำงานที่กว้างสูงสุด 1440px ภายใน ส่วนมือถือเปลี่ยนเป็น top navigation แถวเดียว สรุปยอดเลื่อนแนวนอนได้ และตารางซ่อนข้อมูลรองตามลำดับความสำคัญ

## Components

- Summary metrics: กลุ่มเดียวมีเส้นแบ่ง ไม่แยกเป็นการ์ดลอยจำนวนมาก
- Balance panel: แผงสว่างมีเส้นขอบเป็น anchor ของหน้า พร้อม quick actions ฝากและถอน (พื้นเข้มสงวนไว้ให้แถบนำทางเท่านั้น)
- Transaction table: แถวสูง 72px, สถานะชัด, จำนวนเงินจัดชิดขวา, มี action ต่อแถว
- Transaction dialog: เลือกลูกค้า กรอกจำนวนและบันทึก แล้วตรวจสอบสรุปก่อนยืนยัน
- Customer directory: ตารางเต็มบน desktop และ compact account rows บนมือถือ เห็นยอดคงเหลือกับยอดที่ถอนได้ทันที
- Customer row actions: ปุ่ม “ฝาก C2C” เขียวและ “ถอน C2C” แดงอยู่ข้างกันในแถว เปิด flow ตรงโดยไม่ผ่านขั้นเลือกรูปแบบ
- C2C transactions: หน้าแยกแบบ master-detail สำหรับค้นหา poll สถานะ ดู parts และยกเลิกรายการที่ยัง PENDING โดยไม่แสดงรายละเอียดบัญชีบุคคลที่สามในรายการรวม
- Toast: ยืนยันผลแบบสั้นและไม่บังเนื้อหา

## Motion

ใช้ 160–220ms เพื่อแสดง hover, dialog และ feedback เท่านั้น ปิด transform และ transition ที่ไม่จำเป็นเมื่อ prefers-reduced-motion
